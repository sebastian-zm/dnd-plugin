{
  "name": "get_npc",
  "description": "Returns the full stat block for an NPC, looked up by slug or UUID.",
  "parameters": {
    "type": "object",
    "required": ["game", "npc"],
    "properties": {
      "game": {
        "type": "string",
        "description": "The slug of the game."
      },
      "npc": {
        "type": "string",
        "description": "The slug or UUID of the NPC."
      }
    }
  }
}
