{
  "name": "list_characters",
  "description": "Lists all player characters in a game. Returns slug, name, current_hp, max_hp, temporary_hp, and ac by default. Pass fields to request additional or different columns.",
  "parameters": {
    "type": "object",
    "required": ["game"],
    "properties": {
      "game": {
        "type": "string",
        "description": "The slug of the game."
      },
      "fields": {
        "type": "array",
        "items": { "type": "string" },
        "description": "Optional list of column names to include in each result. Defaults to [slug, name, current_hp, max_hp, temporary_hp, ac]."
      }
    }
  }
}
