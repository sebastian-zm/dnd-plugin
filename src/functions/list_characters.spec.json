{
  "name": "list_characters",
  "description": "Lists all player characters in a game with a summary (slug, name, player, class, level, AC, HP). For a full sheet, look up the character by slug.",
  "parameters": {
    "type": "object",
    "required": ["game"],
    "properties": {
      "game": {
        "type": "string",
        "description": "The slug of the game."
      }
    }
  }
}
